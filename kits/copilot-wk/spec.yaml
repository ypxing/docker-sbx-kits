schemaVersion: "1"
kind: mixin
name: copilot-wk
displayName: Copilot-Workspace
description: Workspace mixin for Copilot. Provides a heads-up display for Copilot agents, showing model info, usage stats, and more.

network:
  allowedDomains:
    - "raw.githubusercontent.com"

commands:
  install:
    - command: |
        curl -fsSL https://raw.githubusercontent.com/ypxing/coding-crew/main/bootstrap.sh | bash -s -- copilot --version v1.8.5
      user: "1000"
      description: "Install coding-crew"
